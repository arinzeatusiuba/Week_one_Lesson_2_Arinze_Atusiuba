const numValue = 7; // Replace 7 with any numeric value you want to check

if (numValue >= 5 && numValue <= 10) {
  console.log('value is in the specified range');  // shows value is in the specified range
} else {
  console.log('value is outside the specified range');
}